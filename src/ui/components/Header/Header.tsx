import { HeaderContainer, Logo } from "./Header.style";

function Header() {
  return (
    <HeaderContainer>
      <Logo src="/images/logo.svg" />
    </HeaderContainer>
  );
}

export default Header;
